// WindowModel.qml
import QtQuick 2.15

ListModel {
    // Same structure as DoorModel, but you may add a “transparent” flag.
    // Required fields: wallId, x, y, width, height
    // Example entry:
    // { "wallId": 2, "x": 2500, "y": 0, "width": 1200, "height": 1500 }
}