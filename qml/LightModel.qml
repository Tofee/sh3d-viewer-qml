// LightModel.qml
import QtQuick 2.15

ListModel {
    // Light source for the scene.
    // Required fields: type (point|spot|directional), x, y, z, intensity, color
    // Example entry:
    // { "type": "point", "x": 2000, "y": 2000, "z": 2500, "intensity": 1.0, "color": "#ffffff" }
}