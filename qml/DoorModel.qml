// DoorModel.qml
import QtQuick 2.15

ListModel {
    // Door belongs to a wall (wallId) and has its own dimensions.
    // Required fields: wallId, x, y, width, height
    // Example entry:
    // { "wallId": 3, "x": 1500, "y": 0, "width": 900, "height": 2100 }
}