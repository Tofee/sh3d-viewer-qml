// CameraModel.qml
import QtQuick 2.15

ListModel {
    // Optional saved camera view.
    // Required fields: x, y, z, rx, ry, rz (rotation in degrees)
    // Example entry:
    // { "x": 3000, "y": 3000, "z": 2500, "rx": -30, "ry": 45, "rz": 0 }
}